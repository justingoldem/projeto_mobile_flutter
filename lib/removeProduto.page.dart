import 'package:flutter/material.dart';
// Função principal do aplicativo Flutter
void main() {
  runApp(removeProduto()); // Inicia a aplicação com o widget 'removeProduto' como a tela principal
}

// Define a classe 'removeProduto' como um StatelessWidget (sem estado)
class removeProduto extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Placeholder(); // Exibe um widget Placeholder como conteúdo inicial
  }
}
